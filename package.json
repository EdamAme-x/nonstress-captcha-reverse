{
  "name": "nr-w",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.2.5",
    "wrangler": "^4.54.0"
  }
}
